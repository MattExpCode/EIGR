Enhanced Influence-aware Group Recommendation (EIGR)

Requirement:
Python 3.7
Pytorch 1.12.1


Running program:
Using an editor (such as PyCharm) to create a Python project in the EIGR directory and import the source code
Setting parameters as stated in the paper and calling the function in test.py (Some file paths need to be reassigned based on the current runtime environment).
